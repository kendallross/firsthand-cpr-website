import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL("https://firsthandcprtraining.com"),
  title: "FirstHand CPR Training | Real Experience. Real Training.",
  description:
    "CPR, AED, first-aid, and BLS training led by firefighters, paramedics, and EMTs with real-world emergency experience.",
  openGraph: {
    title: "FirstHand CPR Training",
    description: "Real Experience. Real Training.",
    images: [{ url: "/og.png", width: 1200, height: 630 }],
  },
  twitter: {
    card: "summary_large_image",
    title: "FirstHand CPR Training",
    description: "Real Experience. Real Training.",
    images: ["/og.png"],
  },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
